const themes = {
  prairie: 'prairie',
  desert: 'desert',
  arctic: 'arctic',
  mountain: 'mountain',
  /*random: function () {
    const length = Object.keys(this).length - 1;
    const random = Math.floor(Math.random() * Object.keys(this).length);
    ... need to create some random stuff
  },*/
};

export default themes;
